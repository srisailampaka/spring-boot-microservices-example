package com.bank.gateway.security;

/**
 * Placeholder class retained to avoid compilation errors in environments
 * where a servlet-based security configuration was previously present.
 * The reactive `GatewayJwtGlobalFilter` provides the gateway JWT enforcement.
 */
public final class GatewayJwtFilter {
    // intentionally empty
}
