package com.bank.gateway.security;

import org.springframework.context.annotation.Configuration;

/**
 * Minimal placeholder configuration for the gateway. The reactive
 * `GatewayJwtGlobalFilter` enforces JWT validation for proxied routes.
 */
@Configuration
public class GatewaySecurityConfig {
    // intentionally empty
}
